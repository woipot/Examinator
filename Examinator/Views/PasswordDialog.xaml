<Window x:Class="Examinator.Views.PasswordDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Examinator.Views"
        mc:Ignorable="d"
        Title="Пароль для входа" Height="200" Width="300">
    <Border Margin="10" Padding="10">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
        
            <TextBlock Grid.Row ="0" Text="Введите пароль:" TextAlignment="Center"/>
            <PasswordBox Grid.Row ="1" x:Name="ResponseTextBox" Margin="0,20" />
            <Button  Grid.Row ="2" Content="Войти" Click="OKButton_Click" />
        </Grid>
    </Border>
</Window>
